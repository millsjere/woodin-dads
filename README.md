
  # Father's Day Campaign Flow

  This is a code bundle for Father's Day Campaign Flow. The original project is available at https://www.figma.com/design/DVxeWR14lzEDv8X8wcWceZ/Father-s-Day-Campaign-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  